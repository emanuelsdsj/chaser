{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Chaser","text":"<p>A fast, reliable web crawling framework built on modern Python asyncio.</p> <p>HTTP/2 by default. Pydantic-validated items. Bloom filter dedup. No legacy event loop libraries \u2014 just plain asyncio.</p>"},{"location":"#why-chaser","title":"Why Chaser","text":"<p>Most Python crawling tools were designed before modern asyncio existed. They work, but they carry historical baggage: legacy async runtimes, no type hints, items that are basically untyped dicts, and dedup that eats RAM on large crawls.</p> <p>Chaser is a clean rewrite with none of that baggage:</p> <ul> <li>HTTP/2 by default \u2014 multiplexed connections via httpx, lower latency per domain</li> <li>Typed items \u2014 define your schema with Pydantic; invalid data never reaches your store</li> <li>Bloom filter dedup \u2014 O(1) memory per URL regardless of crawl size</li> <li>Pure asyncio \u2014 no Twisted, no greenlets, no legacy runtime to learn</li> </ul>"},{"location":"#install","title":"Install","text":"<pre><code>pip install chaser\n</code></pre> <p>With optional extras:</p> <pre><code>pip install \"chaser[browser]\"    # Playwright for JS-heavy pages\npip install \"chaser[api]\"        # REST API server\npip install \"chaser[metrics]\"    # Prometheus metrics\npip install \"chaser[db]\"         # async SQLAlchemy store\npip install \"chaser[redis]\"      # distributed Redis frontier\npip install \"chaser[parquet]\"    # Parquet output via pyarrow\npip install \"chaser[cloud]\"      # S3 and GCS stores\n</code></pre>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>import asyncio\nfrom chaser import Engine, Item, Request, Trapper\n\n\nclass QuoteItem(Item):\n    text: str\n    author: str\n\n\nclass QuoteTrapper(Trapper):\n    start_urls = [\"https://quotes.toscrape.com\"]\n\n    async def parse(self, response):\n        for quote in response.selector.css(\"div.quote\"):\n            yield QuoteItem(\n                text=quote.css(\"span.text::text\").get(\"\"),\n                author=quote.css(\"small.author::text\").get(\"\"),\n            )\n\n        next_page = response.selector.css(\"li.next a::attr(href)\").get()\n        if next_page:\n            yield Request(url=response.urljoin(next_page))\n\n\nasync def main():\n    engine = Engine(concurrency=4)\n    items = await engine.run(QuoteTrapper())\n    print(engine.stats)\n\nasyncio.run(main())\n</code></pre> <p>That's it. No configuration files required, no class registration, no framework ceremony.</p>"},{"location":"#whats-next","title":"What's next","text":"<ul> <li>Getting Started \u2014 a full walk-through from install to first crawl</li> <li>Trappers \u2014 define what to fetch and how to parse it</li> <li>Pipeline &amp; Stores \u2014 route items to JSONL, CSV, S3, GCS, or a database</li> <li>Hooks \u2014 rate limiting, retries, proxies, robots.txt</li> <li>Browser Rendering \u2014 scrape JavaScript-heavy pages with Playwright</li> </ul>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to this project will be documented in this file.</p> <p>Format: Keep a Changelog Versioning: Semantic Versioning</p>"},{"location":"changelog/#unreleased","title":"[Unreleased]","text":""},{"location":"changelog/#001-2025-05-03","title":"[0.0.1] \u2014 2025-05-03","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li><code>Engine</code> \u2014 async hub coordinating frontier, net client, trap layer, and pipeline</li> <li><code>Frontier</code> \u2014 bloom filter deduplication (MurmurHash3 + bitarray) with BFS/DFS/score scheduling strategies</li> <li><code>NetClient</code> \u2014 httpx connection pool with HTTP/2 by default, per-domain circuit breaker, SOCKS5 proxy support</li> <li><code>Trapper</code> \u2014 abstract base class for defining crawl targets</li> <li><code>CrawlTrapper</code> \u2014 auto-follows <code>&lt;a&gt;</code> links with <code>allowed_domains</code> and <code>deny_extensions</code> filtering</li> <li><code>SitemapTrapper</code> \u2014 sitemap-driven crawl, recursively follows <code>&lt;sitemapindex&gt;</code> entries</li> <li><code>Item</code> \u2014 Pydantic v2 base model for validated scraped items</li> <li><code>ItemLoader</code> \u2014 field processor chains (<code>strip</code>, <code>join</code>, <code>first</code>, <code>take_all</code>, <code>compose</code>)</li> <li><code>Selector</code> / <code>SelectorList</code> \u2014 CSS, XPath, JMESPath, and regex extraction (wraps parsel)</li> <li><code>Pipeline</code> + <code>Stage</code> \u2014 async item processing chain</li> <li><code>JsonlStore</code> \u2014 streaming JSONL output with append-safe concurrent writes</li> <li><code>CsvStore</code> \u2014 streaming CSV with auto-header detection</li> <li><code>DbStore</code> \u2014 async SQLAlchemy sink with auto table creation (via <code>chaser[db]</code> extra)</li> <li><code>RetryPolicy</code> \u2014 exponential backoff with jitter</li> <li><code>RateLimitHook</code> \u2014 per-domain token bucket rate limiting</li> <li><code>CookieJarHook</code> \u2014 per-domain cookie jar, reads <code>Set-Cookie</code>, injects on next requests</li> <li><code>RobotsHook</code> \u2014 cached <code>robots.txt</code> compliance per domain</li> <li><code>ProxyPool</code> \u2014 round-robin proxy rotation with failure tracking</li> <li><code>BrowserClient</code> \u2014 Playwright-based fetch client returning same <code>Response</code> interface (via <code>chaser[browser]</code> extra)</li> <li><code>ChaserSettings</code> \u2014 configuration via <code>[tool.chaser]</code> in <code>pyproject.toml</code> and <code>CHASER_*</code> env vars</li> <li>CLI: <code>chaser run</code>, <code>chaser shell</code>, <code>chaser version</code></li> <li><code>Response.urljoin()</code> for relative URL resolution</li> <li><code>Response.json_selector</code> property for JMESPath queries on JSON responses</li> </ul>"},{"location":"cli/","title":"CLI Reference","text":"<p>The <code>chaser</code> command-line tool lets you scaffold projects, run Trappers, start the API server, and open an interactive shell.</p>"},{"location":"cli/#chaser-new","title":"chaser new","text":"<p>Scaffold a new project directory:</p> <pre><code>chaser new myproject\n</code></pre> <p>Creates:</p> <pre><code>myproject/\n\u251c\u2500\u2500 pyproject.toml\n\u251c\u2500\u2500 myproject/\n\u2502   \u251c\u2500\u2500 __init__.py\n\u2502   \u2514\u2500\u2500 trappers.py      # starter Trapper\n\u2514\u2500\u2500 tests/\n    \u2514\u2500\u2500 test_trappers.py\n</code></pre>"},{"location":"cli/#chaser-run","title":"chaser run","text":"<p>Run a Trapper by dotted import path:</p> <pre><code>chaser run myproject.trappers.MyTrapper\n</code></pre> <p>Options:</p> Flag Description <code>--concurrency N</code> Number of parallel workers (default: 16) <code>--log-level LEVEL</code> Logging level: DEBUG, INFO, WARNING, ERROR (default: INFO) <code>--json-logs</code> Emit structured JSON log lines instead of plain text <code>--output FILE</code> Write items to a JSONL file <code>--no-http2</code> Disable HTTP/2 <pre><code>chaser run myproject.trappers.BlogTrapper \\\n  --concurrency 32 \\\n  --output articles.jsonl \\\n  --log-level DEBUG\n</code></pre>"},{"location":"cli/#chaser-serve","title":"chaser serve","text":"<p>Start the REST API server:</p> <pre><code>chaser serve\n</code></pre> <p>Options:</p> Flag Default Description <code>--host HOST</code> <code>0.0.0.0</code> Interface to bind <code>--port PORT</code> <code>8000</code> Port to listen on <code>--reload</code> off Auto-reload on code changes (development) <pre><code>chaser serve --port 9000 --reload\n</code></pre> <p>Requires <code>pip install \"chaser[api]\"</code>.</p>"},{"location":"cli/#chaser-shell","title":"chaser shell","text":"<p>Open an interactive Python shell with a live <code>Engine</code> and convenience imports pre-loaded. Useful for experimenting with selectors against real pages.</p> <pre><code>chaser shell\n</code></pre> <p>Inside the shell:</p> <pre><code>&gt;&gt;&gt; response = await fetch(\"https://example.com\")\n&gt;&gt;&gt; response.selector.css(\"title::text\").get()\n'Example Domain'\n&gt;&gt;&gt; response.status\n200\n</code></pre>"},{"location":"cli/#chaser-version","title":"chaser version","text":"<p>Print the installed version:</p> <pre><code>chaser version\n# chaser 0.4.0\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<pre><code>pip install chaser\n</code></pre> <p>Python 3.11 or higher is required.</p>"},{"location":"getting-started/#your-first-trapper","title":"Your first Trapper","text":"<p>A Trapper is a class that tells Chaser what to fetch and how to parse it. Define one by subclassing <code>Trapper</code>, setting <code>start_urls</code>, and writing a <code>parse</code> method that yields items and follow-up requests.</p> <pre><code>import asyncio\nfrom chaser import Engine, Item, Request, Trapper\n\n\nclass QuoteItem(Item):\n    text: str\n    author: str\n\n\nclass QuoteTrapper(Trapper):\n    start_urls = [\"https://quotes.toscrape.com\"]\n\n    async def parse(self, response):\n        for quote in response.selector.css(\"div.quote\"):\n            yield QuoteItem(\n                text=quote.css(\"span.text::text\").get(\"\"),\n                author=quote.css(\"small.author::text\").get(\"\"),\n            )\n\n        next_page = response.selector.css(\"li.next a::attr(href)\").get()\n        if next_page:\n            yield Request(url=response.urljoin(next_page))\n\n\nasync def main():\n    engine = Engine(concurrency=4)\n    items = await engine.run(QuoteTrapper())\n    for item in items:\n        print(f\"{item.author}: {item.text[:60]}\")\n    print(engine.stats)\n\n\nasyncio.run(main())\n</code></pre>"},{"location":"getting-started/#whats-happening-here","title":"What's happening here","text":"<ol> <li><code>QuoteItem</code> defines the data schema \u2014 any item that doesn't match the types raises a <code>ValidationError</code> before it reaches your store.</li> <li><code>QuoteTrapper.parse</code> uses CSS selectors (via <code>response.selector</code>) to extract data and yields <code>QuoteItem</code> objects.</li> <li>Yielding a <code>Request</code> re-enqueues a URL back into the Frontier; Chaser deduplicates it automatically using a bloom filter.</li> <li><code>Engine(concurrency=4)</code> runs 4 workers in parallel, sharing a single connection pool.</li> </ol>"},{"location":"getting-started/#run-from-the-cli","title":"Run from the CLI","text":"<p>Save your Trapper to <code>myproject/trappers.py</code> and run it:</p> <pre><code>chaser run myproject.trappers.QuoteTrapper\n</code></pre> <p>Or scaffold a complete project first:</p> <pre><code>chaser new myproject\ncd myproject\nchaser run myproject.trappers.QuoteTrapper\n</code></pre>"},{"location":"getting-started/#saving-items-to-a-file","title":"Saving items to a file","text":"<p>By default <code>engine.run()</code> collects items in memory and returns them. To stream to disk instead, attach a pipeline:</p> <pre><code>from chaser import Engine, JsonlStore, Pipeline\n\npipeline = Pipeline([JsonlStore(\"quotes.jsonl\")])\nengine = Engine(pipeline=pipeline)\nawait engine.run(QuoteTrapper())\n</code></pre> <p>Items are written to <code>quotes.jsonl</code> as they arrive \u2014 no buffering in memory.</p>"},{"location":"getting-started/#what-to-read-next","title":"What to read next","text":"Goal Guide Crawl an entire domain Trappers \u2014 CrawlTrapper Validate and transform items Items &amp; Loaders Store to CSV, database, S3 Pipeline &amp; Stores Rate limiting, retries, proxies Hooks Scrape JavaScript pages Browser Rendering Resume a crashed crawl Crawl Resume"},{"location":"net/","title":"Net Layer","text":"<p>The <code>chaser.net</code> package is the foundation of every crawl \u2014 it defines how requests are described and how responses are represented.</p>"},{"location":"net/#request","title":"Request","text":"<p><code>Request</code> is a dataclass that describes a single fetch operation.</p> <pre><code>from chaser.net.request import Request\n\nreq = Request(url=\"https://example.com\")\nreq = Request(\n    url=\"https://example.com/login\",\n    method=\"POST\",\n    headers={\"Content-Type\": \"application/json\"},\n    body=b'{\"user\": \"me\"}',\n    meta={\"source\": \"seed\"},\n    priority=10,\n    callback=\"parse_login\",\n    use_browser=False,\n)\n</code></pre> Field Type Default Description <code>url</code> <code>str</code> \u2014 Target URL <code>method</code> <code>str</code> <code>\"GET\"</code> HTTP method (auto-uppercased) <code>headers</code> <code>Headers</code> <code>{}</code> Request headers <code>body</code> <code>bytes \\| None</code> <code>None</code> Request body <code>meta</code> <code>dict</code> <code>{}</code> Arbitrary data passed through to the response <code>priority</code> <code>int</code> <code>0</code> Higher = fetched sooner <code>callback</code> <code>str \\| None</code> <code>None</code> Trapper method name to call with the response <code>use_browser</code> <code>bool</code> <code>False</code> Route to Playwright instead of httpx <p>Use <code>request.copy(**overrides)</code> to produce a modified copy without mutating the original.</p>"},{"location":"net/#response","title":"Response","text":"<p><code>Response</code> wraps the raw HTTP response with convenient accessors.</p> <pre><code>from chaser.net.response import Response\nfrom chaser.net.headers import Headers\n\nres = Response(\n    url=\"https://example.com\",\n    status=200,\n    headers=Headers({\"content-type\": \"text/html\"}),\n    body=b\"&lt;html&gt;...&lt;/html&gt;\",\n    elapsed=0.34,\n)\n\nprint(res.text)      # decoded string\nprint(res.ok)        # True if 200\u2013299\nprint(res.json())    # parsed JSON (raises if not JSON)\n</code></pre> Field Type Default Description <code>url</code> <code>str</code> \u2014 Final URL after redirects <code>status</code> <code>int</code> \u2014 HTTP status code <code>headers</code> <code>Headers</code> \u2014 Response headers <code>body</code> <code>bytes</code> \u2014 Raw response body <code>encoding</code> <code>str</code> <code>\"utf-8\"</code> Charset for <code>.text</code> decoding <code>elapsed</code> <code>float</code> <code>0.0</code> Seconds taken to fetch <code>request</code> <code>Request \\| None</code> <code>None</code> The request that produced this response <p><code>.css()</code> and <code>.xpath()</code> shortcuts are added in the <code>extract</code> phase.</p>"},{"location":"net/#headers","title":"Headers","text":"<p><code>Headers</code> is a case-insensitive, multi-value HTTP headers container.</p> <pre><code>from chaser.net.headers import Headers\n\nh = Headers({\"Content-Type\": \"text/html\", \"X-Custom\": \"value\"})\nassert h[\"content-type\"] == \"text/html\"\nassert \"CONTENT-TYPE\" in h\n</code></pre>"},{"location":"net/#multi-value-headers","title":"Multi-value headers","text":"<p>Some headers (e.g. <code>Set-Cookie</code>) can appear multiple times. Use <code>getlist()</code> to retrieve all values:</p> <pre><code>h = Headers({\"Set-Cookie\": \"a=1\"})\nh.add(\"Set-Cookie\", \"b=2\")\nh.add(\"Set-Cookie\", \"c=3\")\n\nh[\"set-cookie\"]          # \"c=3\"  \u2014 last value (dict-style access)\nh.getlist(\"set-cookie\")  # [\"a=1\", \"b=2\", \"c=3\"]\n</code></pre>"},{"location":"net/#api","title":"API","text":"Method Description <code>h[key]</code> Return the last value for <code>key</code> (case-insensitive) <code>h[key] = value</code> Replace all values for <code>key</code> with a single <code>value</code> <code>h.add(key, value)</code> Append <code>value</code> without replacing existing values <code>h.getlist(key)</code> Return all values for <code>key</code> as a list; <code>[]</code> if absent <code>h.to_dict_list()</code> Serialise to <code>dict[str, list[str]]</code>, preserving all values <code>key in h</code> Membership test (case-insensitive)"},{"location":"cloud/api/","title":"REST API","text":"<p>The Chaser API server manages crawl jobs over HTTP. Start it, submit Trapper classes by dotted path, and poll for status and items \u2014 all from curl, a browser, or any HTTP client.</p>"},{"location":"cloud/api/#setup","title":"Setup","text":"<pre><code>pip install \"chaser[api]\"\nchaser serve\n</code></pre> <p>The server starts on <code>http://localhost:8000</code> by default. Port can be changed:</p> <pre><code>chaser serve --port 9000\n</code></pre>"},{"location":"cloud/api/#endpoints","title":"Endpoints","text":""},{"location":"cloud/api/#start-a-crawl","title":"Start a crawl","text":"<pre><code>POST /crawls\n</code></pre> <pre><code>curl -X POST http://localhost:8000/crawls \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"trapper\": \"mymodule.trappers.QuoteTrapper\",\n    \"concurrency\": 8\n  }'\n</code></pre> <p>Request body</p> Field Type Default Description <code>trapper</code> <code>string</code> required Dotted import path to the Trapper class <code>concurrency</code> <code>int</code> <code>16</code> Number of parallel workers <code>http2</code> <code>bool</code> <code>true</code> Enable HTTP/2 <code>timeout</code> <code>float</code> <code>30.0</code> Per-request timeout in seconds <code>proxy</code> <code>string</code> <code>null</code> Proxy URL for all requests <code>cache_dir</code> <code>string</code> <code>null</code> Path to HTTP cache directory <code>frontier_db</code> <code>string</code> <code>null</code> Path to SQLite frontier for resume <p>Response \u2014 <code>201 Created</code></p> <pre><code>{\n  \"id\": \"a1b2c3d4\",\n  \"status\": \"running\",\n  \"trapper\": \"mymodule.trappers.QuoteTrapper\",\n  \"stats\": { \"requests_sent\": 0, \"items_scraped\": 0, ... },\n  \"items_count\": 0,\n  \"error\": null\n}\n</code></pre>"},{"location":"cloud/api/#list-all-jobs","title":"List all jobs","text":"<pre><code>GET /crawls\n</code></pre> <p>Returns an array of all jobs (running, finished, failed).</p>"},{"location":"cloud/api/#get-a-job","title":"Get a job","text":"<pre><code>GET /crawls/{id}\n</code></pre> <pre><code>curl http://localhost:8000/crawls/a1b2c3d4\n</code></pre> <p><code>status</code> is one of <code>running</code>, <code>finished</code>, or <code>failed</code>.</p>"},{"location":"cloud/api/#cancel-a-job","title":"Cancel a job","text":"<pre><code>DELETE /crawls/{id}\n</code></pre> <p>Cancels the asyncio task. Already-collected items are preserved.</p>"},{"location":"cloud/api/#get-items","title":"Get items","text":"<pre><code>GET /crawls/{id}/items?offset=0&amp;limit=100\n</code></pre> <p>Returns paginated items collected so far. Available while the job is still running.</p> <pre><code>curl \"http://localhost:8000/crawls/a1b2c3d4/items?limit=50\"\n</code></pre>"},{"location":"cloud/api/#stats-fields","title":"Stats fields","text":"<p>The <code>stats</code> object in every response:</p> Field Description <code>requests_sent</code> Total requests dispatched <code>requests_ok</code> Successful responses (2xx) <code>requests_failed</code> Failed requests (error or 4xx/5xx) <code>cache_hits</code> Responses served from HTTP cache <code>items_scraped</code> Items yielded by Trappers <code>bytes_downloaded</code> Total bytes received <code>timeouts</code> Requests that timed out <code>errors_by_status</code> Dict of HTTP status code \u2192 count <code>elapsed</code> Seconds since the job started <code>requests_per_second</code> Throughput over <code>elapsed</code>"},{"location":"cloud/api/#interactive-docs","title":"Interactive docs","text":"<p>The API server exposes Swagger UI at <code>http://localhost:8000/docs</code> and ReDoc at <code>http://localhost:8000/redoc</code>.</p>"},{"location":"cloud/api/#prometheus-metrics","title":"Prometheus metrics","text":"<p>When <code>chaser[metrics]</code> is also installed, the server exposes a <code>/metrics</code> endpoint in Prometheus text format. See Prometheus Metrics for details.</p>"},{"location":"cloud/metrics/","title":"Prometheus Metrics","text":"<p>Chaser exposes crawl metrics in standard Prometheus text format. Every metric is tagged with a <code>job</code> label so multiple concurrent crawls are tracked without collision.</p>"},{"location":"cloud/metrics/#setup","title":"Setup","text":"<pre><code>pip install \"chaser[api,metrics]\"\nchaser serve\n</code></pre> <p>Metrics are available at:</p> <pre><code>GET http://localhost:8000/metrics\n</code></pre>"},{"location":"cloud/metrics/#available-metrics","title":"Available metrics","text":"Metric Type Description <code>chaser_requests_total</code> Counter Total requests by result (<code>ok</code>, <code>timeout</code>, <code>failed</code>, <code>circuit_open</code>, <code>aborted</code>, <code>cached</code>) <code>chaser_items_scraped_total</code> Counter Items yielded by Trappers <code>chaser_bytes_downloaded_total</code> Counter Raw bytes received from servers <code>chaser_request_duration_seconds_p99</code> Gauge 99th percentile request latency <code>chaser_frontier_queue_size</code> Gauge Current pending URL count <code>chaser_frontier_seen_urls</code> Gauge Total distinct URLs seen <code>chaser_http_errors_total</code> Counter HTTP error responses by status code <p>All metrics carry a <code>job</code> label matching the <code>job_name</code> passed to the Engine.</p>"},{"location":"cloud/metrics/#example-output","title":"Example output","text":"<pre><code># HELP chaser_requests_total Total requests dispatched\n# TYPE chaser_requests_total counter\nchaser_requests_total{job=\"product_crawl\", result=\"ok\"} 1423.0\nchaser_requests_total{job=\"product_crawl\", result=\"timeout\"} 3.0\n\n# HELP chaser_items_scraped_total Total items scraped\n# TYPE chaser_items_scraped_total counter\nchaser_items_scraped_total{job=\"product_crawl\"} 891.0\n\n# HELP chaser_bytes_downloaded_total Total bytes downloaded\n# TYPE chaser_bytes_downloaded_total counter\nchaser_bytes_downloaded_total{job=\"product_crawl\"} 8473291.0\n\n# HELP chaser_frontier_queue_size Current frontier queue size\n# TYPE chaser_frontier_queue_size gauge\nchaser_frontier_queue_size{job=\"product_crawl\"} 342.0\n\n# HELP chaser_http_errors_total HTTP error responses by status code\n# TYPE chaser_http_errors_total counter\nchaser_http_errors_total{job=\"product_crawl\", status_code=\"429\"} 18.0\n</code></pre>"},{"location":"cloud/metrics/#standalone-mode-without-the-api-server","title":"Standalone mode (without the API server)","text":"<p>Use <code>ChaserMetrics</code> directly in a script:</p> <pre><code>import asyncio\nfrom chaser import Engine\nfrom chaser.metrics import ChaserMetrics\n\nmetrics = ChaserMetrics()\nengine = Engine(metrics=metrics, job_name=\"product_crawl\")\nawait engine.run(MyTrapper())\n</code></pre> <p>In standalone mode, metrics accumulate in memory. Expose them with the built-in ASGI app if you want to scrape them:</p> <pre><code>import uvicorn\nfrom chaser.metrics import ChaserMetrics\n\nmetrics = ChaserMetrics()\napp = metrics.make_asgi_app()\nuvicorn.run(app, host=\"0.0.0.0\", port=9090)\n</code></pre>"},{"location":"cloud/metrics/#grafana-dashboard","title":"Grafana dashboard","text":"<p>A minimal Grafana dashboard can be built from these series. Recommended panels:</p> <ul> <li>Throughput: rate of <code>chaser_requests_total{result=\"ok\"}</code> over 1m</li> <li>Error rate: rate of <code>chaser_http_errors_total</code> over 1m</li> <li>Queue depth: <code>chaser_frontier_queue_size</code> \u2014 dropping to 0 means the crawl is finishing</li> <li>Latency p99: <code>chaser_request_duration_seconds_p99</code></li> <li>Items/s: rate of <code>chaser_items_scraped_total</code> over 1m</li> </ul>"},{"location":"cloud/redis/","title":"Redis Frontier","text":"<p>The default in-memory frontier and the SQLite frontier are single-process. For crawls that span multiple machines or processes, use the <code>RedisFrontier</code> \u2014 all workers connect to the same Redis server and coordinate atomically.</p>"},{"location":"cloud/redis/#setup","title":"Setup","text":"<pre><code>pip install \"chaser[redis]\"\n</code></pre> <p>Pass the Redis URL to the Engine:</p> <pre><code>engine = Engine(frontier_redis=\"redis://localhost:6379\")\nawait engine.run(MyTrapper())\n</code></pre> <p>Multiple processes running the same command connect to the same frontier \u2014 each process contributes workers to the shared queue.</p>"},{"location":"cloud/redis/#how-it-works","title":"How it works","text":"Operation Redis structure Deduplication <code>HSETNX</code> on a hash \u2014 atomic check-and-mark per URL Queue Sorted set (<code>ZADD</code> / <code>BZPOPMIN</code>) \u2014 score encodes BFS/DFS/priority In-flight tracking Hash \u2014 maps slot ID \u2192 request payload Crash recovery On startup, in-flight requests are moved back to the queue"},{"location":"cloud/redis/#scheduling-strategies","title":"Scheduling strategies","text":"<p>The <code>strategy</code> parameter controls how the sorted set score is computed:</p> Strategy Behaviour <code>bfs</code> (default) Insertion counter as score \u2014 breadth-first <code>dfs</code> Negative insertion counter \u2014 depth-first <code>score</code> Negative <code>request.priority</code> \u2014 higher priority fetched first"},{"location":"cloud/redis/#configuration","title":"Configuration","text":"<pre><code>from chaser.frontier.redis_frontier import RedisFrontier\n\nfrontier = RedisFrontier(\n    redis_url=\"redis://localhost:6379\",\n    prefix=\"myproject\",    # namespace for Redis keys (avoids collisions)\n    strategy=\"bfs\",\n    sort_params=False,     # whether to sort query string params during canonicalization\n    clear=False,           # True \u2192 wipe all keys on open() and start fresh\n)\n</code></pre> <p>To use a custom-configured frontier directly:</p> <pre><code>frontier = RedisFrontier(\"redis://myhost:6379\", prefix=\"crawl-2024\")\nengine = Engine(frontier_redis=\"redis://myhost:6379\")  # or pass frontier_redis\n</code></pre>"},{"location":"cloud/redis/#crash-recovery","title":"Crash recovery","text":"<p>When a process dies mid-crawl, requests that were in-flight (popped but not acknowledged) are automatically moved back to the pending queue the next time any process calls <code>open()</code>. No manual intervention needed.</p>"},{"location":"cloud/redis/#multi-process-example","title":"Multi-process example","text":"<p>Run as many worker processes as you need \u2014 they all share the same frontier:</p> <pre><code># Terminal 1\nCHASER_CONCURRENCY=16 python worker.py\n\n# Terminal 2\nCHASER_CONCURRENCY=16 python worker.py\n\n# Terminal 3 \u2014 more workers on a different machine\nCHASER_CONCURRENCY=8 python worker.py\n</code></pre> <pre><code># worker.py\nimport asyncio\nfrom chaser import Engine\nfrom myproject.trappers import MyTrapper\n\nasync def main():\n    engine = Engine(\n        concurrency=16,\n        frontier_redis=\"redis://redis-host:6379\",\n    )\n    await engine.run(MyTrapper())\n\nasyncio.run(main())\n</code></pre>"},{"location":"cloud/redis/#key-namespacing","title":"Key namespacing","text":"<p>Each <code>RedisFrontier</code> uses a <code>prefix</code> to namespace its Redis keys:</p> <ul> <li><code>{prefix}:seen</code> \u2014 dedup hash</li> <li><code>{prefix}:queue</code> \u2014 sorted set of pending requests</li> <li><code>{prefix}:counter</code> \u2014 monotonic counter for BFS/DFS ordering</li> <li><code>{prefix}:inflight</code> \u2014 crash recovery hash</li> </ul> <p>Use a different <code>prefix</code> for each concurrent crawl project to avoid collisions.</p>"},{"location":"cloud/stores/","title":"Cloud Stores","text":"<p>Chaser can write items directly to Amazon S3, Google Cloud Storage, or Parquet files. All three are pipeline stages \u2014 they slot in anywhere the standard <code>JsonlStore</code> would go.</p>"},{"location":"cloud/stores/#amazon-s3","title":"Amazon S3","text":"<p><code>S3Store</code> accumulates items in a local temp file, then uploads it as a single object when the crawl finishes.</p> <pre><code>pip install \"chaser[cloud]\"\n</code></pre> <pre><code>from chaser.pipeline.store.s3 import S3Store\n\npipeline = Pipeline([\n    S3Store(\"my-bucket\", \"crawls/run-001/items.jsonl\")\n])\n</code></pre> <p>Format is inferred from the key extension: <code>.parquet</code> \u2192 Parquet, everything else \u2192 JSONL.</p> <pre><code># Parquet output (requires chaser[parquet] as well)\nS3Store(\"my-bucket\", \"crawls/run-001/items.parquet\")\n</code></pre>"},{"location":"cloud/stores/#constructor-parameters","title":"Constructor parameters","text":"Parameter Description <code>bucket</code> S3 bucket name <code>key</code> Object key (path within the bucket) <code>endpoint_url</code> Override endpoint \u2014 use for MinIO or other S3-compatible services <code>region_name</code> AWS region <code>aws_access_key_id</code> Explicit credentials (prefer IAM roles or env vars in production) <code>aws_secret_access_key</code> Explicit credentials"},{"location":"cloud/stores/#authentication","title":"Authentication","text":"<p>By default <code>S3Store</code> uses the standard boto3 credential chain: <code>AWS_ACCESS_KEY_ID</code> / <code>AWS_SECRET_ACCESS_KEY</code> environment variables, <code>~/.aws/credentials</code>, or the instance IAM role. Explicit credentials in the constructor override the chain.</p>"},{"location":"cloud/stores/#s3-compatible-storage-minio-cloudflare-r2-etc","title":"S3-compatible storage (MinIO, Cloudflare R2, etc.)","text":"<pre><code>S3Store(\n    \"my-bucket\",\n    \"items.jsonl\",\n    endpoint_url=\"http://minio:9000\",\n    aws_access_key_id=\"minioadmin\",\n    aws_secret_access_key=\"minioadmin\",\n)\n</code></pre>"},{"location":"cloud/stores/#google-cloud-storage","title":"Google Cloud Storage","text":"<p><code>GCSStore</code> works the same way \u2014 accumulate locally, upload on close.</p> <pre><code>pip install \"chaser[cloud]\"\n</code></pre> <pre><code>from chaser.pipeline.store.gcs import GCSStore\n\npipeline = Pipeline([\n    GCSStore(\"my-bucket\", \"crawls/run-001/items.jsonl\")\n])\n</code></pre> <p>Format inference is identical to <code>S3Store</code>: <code>.parquet</code> \u2192 Parquet, otherwise JSONL.</p>"},{"location":"cloud/stores/#constructor-parameters_1","title":"Constructor parameters","text":"Parameter Description <code>bucket</code> GCS bucket name <code>blob_name</code> Object path within the bucket <code>project</code> GCP project ID (uses ADC project if omitted) <code>credentials</code> Explicit <code>google.oauth2.credentials.Credentials</code> object"},{"location":"cloud/stores/#authentication_1","title":"Authentication","text":"<p>GCSStore uses Application Default Credentials. Set <code>GOOGLE_APPLICATION_CREDENTIALS</code> to a service account key file, or run <code>gcloud auth application-default login</code> for local development.</p>"},{"location":"cloud/stores/#parquet-local","title":"Parquet (local)","text":"<p>For columnar output without uploading to cloud storage:</p> <pre><code>pip install \"chaser[parquet]\"\n</code></pre> <pre><code>from chaser.pipeline.store.parquet import ParquetStore\n\npipeline = Pipeline([ParquetStore(\"output.parquet\")])\n</code></pre> <p>Items are buffered in memory and written to a single Parquet file on close. The schema is inferred from the items automatically using pyarrow.</p>"},{"location":"cloud/stores/#combining-stores","title":"Combining stores","text":"<pre><code>pipeline = Pipeline([\n    DuplicateFilter(key=lambda i: i.url),\n    JsonlStore(\"local-backup.jsonl\"),        # always write locally\n    S3Store(\"my-bucket\", \"run/items.jsonl\"), # and archive to S3\n])\n</code></pre>"},{"location":"guide/browser/","title":"Browser Rendering","text":"<p>For JavaScript-heavy pages that are empty without a browser, Chaser integrates Playwright. Browser requests run through the same Engine and Pipeline as regular HTTP requests \u2014 you mix both in a single crawl.</p>"},{"location":"guide/browser/#setup","title":"Setup","text":"<pre><code>pip install \"chaser[browser]\"\nplaywright install chromium\n</code></pre>"},{"location":"guide/browser/#per-request-browser-fetching","title":"Per-request browser fetching","text":"<p>Set <code>use_browser=True</code> on any <code>Request</code> and enable the browser in the Engine:</p> <pre><code>from chaser import Engine, Request, Trapper\n\n\nclass SpaTrapper(Trapper):\n    def start_requests(self):\n        return [Request(url=\"https://spa.example.com\", use_browser=True)]\n\n    async def parse(self, response):\n        title = response.selector.css(\"h1::text\").get(\"\")\n        ...\n\n\nengine = Engine(browser=True)\nawait engine.run(SpaTrapper())\n</code></pre> <p>Regular requests (without <code>use_browser=True</code>) are still fetched via the httpx pool \u2014 only flagged requests go through Playwright.</p>"},{"location":"guide/browser/#browser-pool","title":"Browser pool","text":"<p>By default, each browser request opens a new Playwright page and closes it after the response is collected. The <code>BrowserPool</code> reuses pages across requests, which is significantly faster for crawls with many browser requests.</p> <pre><code>from chaser import BrowserPool, Engine\n\npool = BrowserPool(size=4)   # keep 4 pages open concurrently\nengine = Engine(browser=pool)\nawait engine.run(SpaTrapper())\n</code></pre> <p>The pool manages a single browser process with multiple pages. <code>size</code> controls the maximum number of concurrent page slots.</p>"},{"location":"guide/browser/#stealth-mode","title":"Stealth mode","text":"<p>Sites that detect headless browsers can be bypassed with <code>StealthConfig</code>. It rotates user agents, viewports, timezones, and locales, and patches <code>navigator.webdriver</code> so the browser looks more like a real user.</p> <pre><code>from chaser import BrowserPool, Engine, StealthConfig\n\nstealth = StealthConfig(\n    user_agents=[\"Mozilla/5.0 ...\", \"Mozilla/5.0 ...\"],\n    viewports=[(1920, 1080), (1366, 768), (1440, 900)],\n    locales=[\"en-US\", \"en-GB\"],\n    timezones=[\"America/New_York\", \"Europe/London\"],\n)\npool = BrowserPool(size=4, stealth=stealth)\nengine = Engine(browser=pool)\n</code></pre> <p>Each request gets a randomly selected combination of UA, viewport, locale, and timezone. The browser context is isolated per request slot when stealth is enabled.</p>"},{"location":"guide/browser/#har-recording-in-browser-mode","title":"HAR recording in browser mode","text":"<p><code>HarWriter</code> works with both httpx and browser requests:</p> <pre><code>from chaser import Engine, HarWriter\n\nasync with HarWriter(\"crawl.har\") as har:\n    engine = Engine(browser=True, hooks=[har])\n    await engine.run(SpaTrapper())\n</code></pre>"},{"location":"guide/browser/#direct-browserclient","title":"Direct BrowserClient","text":"<p>For scripts that don't need the full Engine, use <code>BrowserClient</code> directly:</p> <pre><code>from chaser import BrowserClient, Request\n\nasync with BrowserClient() as client:\n    response = await client.fetch(Request(url=\"https://example.com\"))\n    print(response.text[:200])\n</code></pre>"},{"location":"guide/cache/","title":"HTTP Cache","text":"<p>Chaser can cache responses to disk and skip re-fetching pages that have not changed. This speeds up iterative development (run your parser repeatedly without hammering the server) and makes partial-failure recovery cheaper.</p>"},{"location":"guide/cache/#enable-caching","title":"Enable caching","text":"<pre><code>engine = Engine(cache_dir=\".cache\")\nawait engine.run(MyTrapper())\n</code></pre> <p>The first run fetches everything and writes responses to <code>.cache/</code>. Subsequent runs serve cached responses instantly for anything that is still fresh.</p>"},{"location":"guide/cache/#cache-behaviour","title":"Cache behaviour","text":"<p>Chaser implements standard HTTP cache semantics:</p> Header Behaviour <code>Cache-Control: max-age=N</code> Cache for N seconds <code>Cache-Control: no-store</code> Never cache <code>ETag</code> / <code>If-None-Match</code> Conditional GET \u2014 304 responses served from cache <code>Last-Modified</code> / <code>If-Modified-Since</code> Conditional GET \u2014 304 responses served from cache <p>Responses without any caching headers are cached for the session (never re-validated during the same run, always re-fetched on the next run).</p>"},{"location":"guide/cache/#cache-hits-in-stats","title":"Cache hits in stats","text":"<p><code>engine.stats.cache_hits</code> counts how many responses were served from the cache:</p> <pre><code>items = await engine.run(MyTrapper())\nprint(f\"Fetched: {engine.stats.requests_ok}, cached: {engine.stats.cache_hits}\")\n</code></pre>"},{"location":"guide/cache/#cache-directory-layout","title":"Cache directory layout","text":"<p>Each response is stored as two files under <code>cache_dir/</code>:</p> <pre><code>.cache/\n\u251c\u2500\u2500 abc123.body      # raw response bytes\n\u2514\u2500\u2500 abc123.meta      # URL, status, headers, timestamp (JSON)\n</code></pre> <p>The filename is derived from the canonical URL. It is safe to delete individual files or the entire directory at any time.</p>"},{"location":"guide/cache/#development-workflow","title":"Development workflow","text":"<p>A typical development cycle with caching:</p> <pre><code># first run \u2014 fetches everything, populates the cache\npython run.py\n\n# subsequent runs \u2014 parses from cache, no network traffic\npython run.py\npython run.py\n\n# reset cache when you want fresh data\nrm -rf .cache/\npython run.py\n</code></pre>"},{"location":"guide/configuration/","title":"Configuration","text":"<p>Chaser reads settings from <code>pyproject.toml</code> and environment variables. The <code>Engine</code> constructor always takes precedence over both \u2014 explicit arguments win.</p>"},{"location":"guide/configuration/#pyprojecttoml","title":"pyproject.toml","text":"<p>Add a <code>[tool.chaser]</code> section to your project's <code>pyproject.toml</code>:</p> <pre><code>[tool.chaser]\nconcurrency = 32\ndownload_delay = 0.5\nuser_agent = \"MyBot/1.0 (+https://example.com/bot)\"\ntimeout = 60.0\nhttp2 = true\n</code></pre>"},{"location":"guide/configuration/#environment-variables","title":"Environment variables","text":"<p>Every setting can also be set via environment variable with the <code>CHASER_</code> prefix:</p> <pre><code>CHASER_CONCURRENCY=32\nCHASER_DOWNLOAD_DELAY=0.5\nCHASER_USER_AGENT=\"MyBot/1.0\"\nCHASER_TIMEOUT=60.0\nCHASER_HTTP2=true\n</code></pre> <p>Environment variables override <code>pyproject.toml</code>. Engine constructor arguments override both.</p>"},{"location":"guide/configuration/#available-settings","title":"Available settings","text":"Setting Type Default Description <code>concurrency</code> <code>int</code> <code>16</code> Number of parallel workers <code>download_delay</code> <code>float</code> <code>0.0</code> Seconds between requests (global) <code>user_agent</code> <code>str</code> httpx default Default <code>User-Agent</code> header <code>timeout</code> <code>float</code> <code>30.0</code> Per-request timeout in seconds <code>http2</code> <code>bool</code> <code>true</code> Enable HTTP/2 <code>max_connections</code> <code>int</code> <code>100</code> Max simultaneous connections in the pool"},{"location":"guide/configuration/#engine-parameters","title":"Engine parameters","text":"<p>The <code>Engine</code> constructor accepts all settings above plus additional runtime options:</p> <pre><code>Engine(\n    concurrency=16,\n    strategy=\"bfs\",        # \"bfs\", \"dfs\", or \"score\"\n    http2=True,\n    timeout=30.0,\n    max_connections=100,\n    proxy=\"socks5://host:1080\",   # global proxy for all requests\n    hooks=[...],\n    retry=RetryPolicy(...),\n    pipeline=Pipeline([...]),\n    browser=True,          # or BrowserPool(...)\n    cache_dir=\".cache\",\n    on_stats=callback,\n    stats_interval=60.0,\n    frontier_db=\"crawl.db\",\n    frontier_redis=\"redis://localhost:6379\",\n    metrics=ChaserMetrics(),\n    job_name=\"my_crawl\",\n)\n</code></pre>"},{"location":"guide/configuration/#logging","title":"Logging","text":"<p>Chaser uses Python's standard <code>logging</code> module under the <code>chaser</code> logger name. To configure it:</p> <pre><code>import logging\n\nlogging.basicConfig(\n    level=logging.INFO,\n    format=\"%(asctime)s %(name)s %(levelname)s %(message)s\",\n)\n</code></pre> <p>Or from the CLI:</p> <pre><code>chaser run mymodule.MyTrapper --log-level DEBUG\nchaser run mymodule.MyTrapper --json-logs   # structured JSON output\n</code></pre>"},{"location":"guide/hooks/","title":"Hooks","text":"<p>Hooks intercept every request and response passing through the Net Client. They run before a request is sent (<code>before_request</code>) and after a response arrives (<code>after_response</code>). Multiple hooks are chained in the order they are passed to the Engine.</p> <pre><code>from chaser import CookieJarHook, Engine, RateLimitHook, RetryPolicy\n\nengine = Engine(\n    hooks=[\n        RateLimitHook(rate=2.0),\n        CookieJarHook(),\n    ],\n    retry=RetryPolicy(max_retries=3),\n)\n</code></pre>"},{"location":"guide/hooks/#ratelimithook","title":"RateLimitHook","text":"<p>Per-domain token bucket. Each domain gets an independent bucket \u2014 throttling one domain never slows another.</p> <pre><code>from chaser import RateLimitHook\n\nRateLimitHook(rate=1.0)   # 1 request/second per domain (default)\nRateLimitHook(rate=5.0, burst=10)  # allow short bursts up to 10\n</code></pre> Parameter Default Description <code>rate</code> <code>1.0</code> Sustained requests per second per domain <code>burst</code> <code>1</code> Maximum token headroom \u2014 controls burst size"},{"location":"guide/hooks/#retrypolicy","title":"RetryPolicy","text":"<p>Exponential backoff with full jitter for transport-level failures (connection errors, timeouts). HTTP 4xx/5xx responses are not retried \u2014 that's application logic.</p> <pre><code>from chaser import RetryPolicy\n\nRetryPolicy(\n    max_retries=3,      # retries after the first attempt\n    base_delay=1.0,     # wait before attempt 1 (doubles each retry)\n    max_delay=60.0,     # cap on computed delay before jitter\n    jitter=True,        # multiply by uniform(0.5, 1.5)\n)\n</code></pre> <p>Delay formula: <code>min(base_delay \u00d7 2^attempt, max_delay) \u00d7 uniform(0.5, 1.5)</code></p>"},{"location":"guide/hooks/#cookiejarhook","title":"CookieJarHook","text":"<p>Maintains a cookie jar per domain. Cookies received in <code>Set-Cookie</code> response headers are automatically sent with subsequent requests to the same domain.</p> <pre><code>from chaser import CookieJarHook\n\nCookieJarHook()\n</code></pre> <p>No configuration needed. Useful for sites that require a session cookie before they serve content.</p>"},{"location":"guide/hooks/#robotshook","title":"RobotsHook","text":"<p>Fetches and caches <code>robots.txt</code> for each domain. Requests to disallowed paths are aborted before they are sent.</p> <pre><code>from chaser import RobotsHook\n\nengine = Engine(hooks=[RobotsHook()])\n</code></pre>"},{"location":"guide/hooks/#proxypool","title":"ProxyPool","text":"<p>Rotates through a list of proxy URLs. On each request, the next proxy in the pool is selected round-robin. Failed proxies can be marked unhealthy automatically.</p> <pre><code>from chaser import ProxyPool\n\nproxies = ProxyPool([\n    \"http://proxy1:8080\",\n    \"http://proxy2:8080\",\n    \"socks5://proxy3:1080\",\n])\nengine = Engine(hooks=[proxies])\n</code></pre>"},{"location":"guide/hooks/#bandwidththrottlehook","title":"BandwidthThrottleHook","text":"<p>Limits download speed across all concurrent workers. Useful when you need to cap bandwidth consumption on a shared connection.</p> <pre><code>from chaser.hooks.bandwidth import BandwidthThrottleHook\n\nengine = Engine(hooks=[BandwidthThrottleHook(max_mbps=10.0)])\n</code></pre>"},{"location":"guide/hooks/#autothrottlehook","title":"AutoThrottleHook","text":"<p>Adjusts request rate dynamically based on server response latency. Backs off when the server is slow, speeds up when it is fast.</p> <pre><code>from chaser import AutoThrottleHook\n\nengine = Engine(hooks=[AutoThrottleHook(target_latency=1.0, max_rate=10.0)])\n</code></pre>"},{"location":"guide/hooks/#harwriter","title":"HarWriter","text":"<p>Records every request and response in HAR 1.2 format. Useful for debugging and replay.</p> <pre><code>from chaser import Engine, HarWriter\n\nasync with HarWriter(\"crawl.har\") as har:\n    engine = Engine(hooks=[har])\n    await engine.run(MyTrapper())\n# crawl.har is flushed and closed automatically on exit\n</code></pre>"},{"location":"guide/hooks/#writing-a-custom-hook","title":"Writing a custom hook","text":"<p>Implement the <code>FetchHook</code> protocol \u2014 one or both methods:</p> <pre><code>from chaser.hooks.base import FetchHook\nfrom chaser.net.request import Request\nfrom chaser.net.response import Response\n\n\nclass TimingHook(FetchHook):\n    async def before_request(self, request: Request) -&gt; Request:\n        # modify the request or just observe it; must return a Request\n        return request\n\n    async def after_response(self, response: Response) -&gt; Response:\n        print(f\"{response.url} \u2014 {response.elapsed:.3f}s\")\n        return response\n</code></pre> <p>To abort a request from <code>before_request</code>, raise <code>RequestAborted</code>:</p> <pre><code>from chaser.hooks.base import FetchHook, RequestAborted\n\nclass BlocklistHook(FetchHook):\n    def __init__(self, blocked: set[str]) -&gt; None:\n        self._blocked = blocked\n\n    async def before_request(self, request: Request) -&gt; Request:\n        from urllib.parse import urlparse\n        if urlparse(request.url).netloc in self._blocked:\n            raise RequestAborted(f\"Blocked: {request.url}\")\n        return request\n</code></pre>"},{"location":"guide/items/","title":"Items &amp; Loaders","text":""},{"location":"guide/items/#defining-an-item","title":"Defining an Item","text":"<p>Items are Pydantic v2 models. Define fields with standard Python type hints \u2014 Pydantic validates every item before it reaches the pipeline.</p> <pre><code>from chaser import Item\n\n\nclass ArticleItem(Item):\n    url: str\n    title: str\n    author: str\n    tags: list[str] = []\n    published_at: str | None = None\n</code></pre> <p>Items with missing required fields or wrong types raise a <code>ValidationError</code> immediately, so invalid data never silently reaches your store.</p>"},{"location":"guide/items/#yielding-items","title":"Yielding items","text":"<p>Return items from any <code>parse</code> method:</p> <pre><code>async def parse(self, response):\n    yield ArticleItem(\n        url=response.url,\n        title=response.selector.css(\"h1::text\").get(\"\"),\n        author=response.selector.css(\".author::text\").get(\"\"),\n        tags=response.selector.css(\".tag::text\").getall(),\n    )\n</code></pre>"},{"location":"guide/items/#itemloader","title":"ItemLoader","text":"<p>For pages where raw selector output needs cleaning before it fits your schema, use <code>ItemLoader</code>. It lets you attach processor functions to each field.</p> <pre><code>from chaser import Item, ItemLoader, compose, first, join, strip\n\n\nclass ArticleItem(Item):\n    url: str\n    title: str\n    body: str\n    tags: list[str]\n\n\nclass ArticleTrapper(Trapper):\n    async def parse(self, response):\n        loader = ItemLoader(ArticleItem, response=response)\n        loader.add_value(\"url\", response.url)\n        loader.add_css(\"title\", \"h1::text\", processor=compose(strip, first()))\n        loader.add_css(\"body\", \".content p::text\", processor=join(\"\\n\"))\n        loader.add_css(\"tags\", \".tag::text\", processor=strip)\n        yield loader.load()\n</code></pre>"},{"location":"guide/items/#built-in-processors","title":"Built-in processors","text":"Processor What it does <code>strip</code> Strips leading/trailing whitespace from each string in the list <code>first()</code> Returns the first element, or a default if the list is empty <code>join(sep)</code> Joins all list elements into one string with <code>sep</code> <code>take_all</code> Identity \u2014 returns the full list (default for list fields) <code>compose(*fns)</code> Chains multiple processors left-to-right"},{"location":"guide/items/#add_css-add_xpath-add_value","title":"add_css / add_xpath / add_value","text":"<pre><code>loader.add_css(\"title\", \"h1::text\")             # CSS selector\nloader.add_xpath(\"author\", \"//span[@class='by']/text()\")  # XPath\nloader.add_value(\"url\", response.url)           # literal value\n</code></pre> <p>Multiple calls to the same field name accumulate values:</p> <pre><code>loader.add_css(\"tags\", \".tag::text\")\nloader.add_css(\"tags\", \".category::text\")\n# tags receives the union of both lists before the processor runs\n</code></pre>"},{"location":"guide/items/#writing-a-custom-processor","title":"Writing a custom processor","text":"<p>A processor is any callable that takes a list and returns a value:</p> <pre><code>def dollar_to_float(values: list[str]) -&gt; float:\n    raw = values[0].replace(\"$\", \"\").replace(\",\", \"\").strip()\n    return float(raw)\n\nloader.add_css(\"price\", \".price::text\", processor=dollar_to_float)\n</code></pre>"},{"location":"guide/pipeline/","title":"Pipeline &amp; Stores","text":"<p>The Pipeline is an async processing chain that every item passes through before being stored. Stages run in order; a stage can transform, filter, or store items.</p>"},{"location":"guide/pipeline/#basic-usage","title":"Basic usage","text":"<pre><code>from chaser import Engine, JsonlStore, Pipeline\n\npipeline = Pipeline([JsonlStore(\"output.jsonl\")])\nengine = Engine(pipeline=pipeline)\nawait engine.run(MyTrapper())\n</code></pre>"},{"location":"guide/pipeline/#built-in-stores","title":"Built-in stores","text":""},{"location":"guide/pipeline/#jsonlstore","title":"JsonlStore","text":"<p>Streams items to a newline-delimited JSON file. Each item is one line.</p> <pre><code>JsonlStore(\"output.jsonl\")\n</code></pre>"},{"location":"guide/pipeline/#csvstore","title":"CsvStore","text":"<p>Streams items to a CSV file. Column names are inferred from the first item's fields.</p> <pre><code>from chaser.pipeline.store.csv import CsvStore\n\nCsvStore(\"output.csv\")\n</code></pre>"},{"location":"guide/pipeline/#dbstore","title":"DbStore","text":"<p>Writes items to a database table using async SQLAlchemy. The table is created automatically from the item's fields if it does not exist.</p> <pre><code>from chaser.pipeline.store.db import DbStore\n\nDbStore(\"sqlite+aiosqlite:///crawl.db\", table=\"articles\")\n# or PostgreSQL:\nDbStore(\"postgresql+asyncpg://user:pass@host/db\", table=\"articles\")\n</code></pre> <p>Requires <code>pip install \"chaser[db]\"</code>.</p>"},{"location":"guide/pipeline/#parquetstore","title":"ParquetStore","text":"<p>Buffers items in memory and writes a single Parquet file on close.</p> <pre><code>from chaser.pipeline.store.parquet import ParquetStore\n\nParquetStore(\"output.parquet\")\n</code></pre> <p>Requires <code>pip install \"chaser[parquet]\"</code>.</p>"},{"location":"guide/pipeline/#filtering-stages","title":"Filtering stages","text":""},{"location":"guide/pipeline/#duplicatefilter","title":"DuplicateFilter","text":"<p>Drops items whose key has already been seen in this crawl run.</p> <pre><code>from chaser.pipeline.filters import DuplicateFilter\n\npipeline = Pipeline([\n    DuplicateFilter(key=lambda item: item.url),\n    JsonlStore(\"output.jsonl\"),\n])\n</code></pre>"},{"location":"guide/pipeline/#dead-letter-queue","title":"Dead-letter queue","text":"<p>Pass <code>dead_letter</code> to capture items that cause any stage to raise an exception. Each failure is appended as a JSON line with the item payload, stage name, and error message \u2014 nothing is silently lost.</p> <pre><code>pipeline = Pipeline(\n    [DuplicateFilter(key=lambda i: i.url), JsonlStore(\"output.jsonl\")],\n    dead_letter=\"failed.jsonl\",\n)\n</code></pre>"},{"location":"guide/pipeline/#writing-a-custom-stage","title":"Writing a custom stage","text":"<p>Subclass <code>Stage</code> and override <code>process</code>. Return <code>None</code> to drop the item.</p> <pre><code>from chaser import Item\nfrom chaser.pipeline.base import Stage\n\n\nclass PriceFilter(Stage):\n    def __init__(self, min_price: float) -&gt; None:\n        self._min = min_price\n\n    async def process(self, item: Item) -&gt; Item | None:\n        if item.price &lt; self._min:   # type: ignore[attr-defined]\n            return None\n        return item\n</code></pre> <p>Use <code>open</code> and <code>close</code> for resources (file handles, DB connections) that should live for the full crawl:</p> <pre><code>class AuditStage(Stage):\n    async def open(self):\n        self._log = open(\"audit.log\", \"a\")\n\n    async def close(self):\n        self._log.close()\n\n    async def process(self, item):\n        self._log.write(f\"{item}\\n\")\n        return item\n</code></pre>"},{"location":"guide/pipeline/#composing-multiple-stores","title":"Composing multiple stores","text":"<p>Stages run in sequence, so you can fan out to multiple stores at once:</p> <pre><code>pipeline = Pipeline([\n    DuplicateFilter(key=lambda i: i.url),\n    JsonlStore(\"output.jsonl\"),   # local backup\n    S3Store(\"my-bucket\", \"run/output.jsonl\"),  # cloud archive\n])\n</code></pre>"},{"location":"guide/resume/","title":"Crawl Resume","text":"<p>Long crawls crash. Servers go down, processes get killed, machines run out of power. The SQLite frontier persists every URL seen and every URL pending so you can pick up exactly where things stopped.</p>"},{"location":"guide/resume/#enable-resume","title":"Enable resume","text":"<p>Pass <code>frontier_db</code> to the Engine:</p> <pre><code>engine = Engine(frontier_db=\"crawl.db\")\nawait engine.run(MyTrapper())\n</code></pre> <p>On the first run, Chaser creates <code>crawl.db</code> and writes all frontier state to it.</p> <p>On the next run with the same <code>frontier_db</code> path: - URLs already visited are skipped (the seen set is restored from the DB) - URLs that were pending but not yet fetched are re-queued - URLs that were in-flight when the process died are moved back to pending automatically</p> <p>No duplicates, no gaps.</p>"},{"location":"guide/resume/#what-gets-persisted","title":"What gets persisted","text":"State Storage Seen URLs (bloom key + canonical form) SQLite <code>seen</code> table Pending queue SQLite <code>queue</code> table In-flight requests (crash recovery) SQLite <code>inflight</code> table"},{"location":"guide/resume/#starting-fresh","title":"Starting fresh","text":"<p>Delete the database file between runs to start a clean crawl:</p> <pre><code>rm crawl.db\n</code></pre> <p>Or rename it to archive the previous crawl state before starting a new one.</p>"},{"location":"guide/resume/#combining-with-the-pipeline","title":"Combining with the pipeline","text":"<p>Resume works independently of the pipeline. Items already extracted in a previous run are not re-extracted (because the URLs are already in the seen set), but the pipeline state (e.g. a JSONL file) accumulates across runs. If you need idempotent output, add a <code>DuplicateFilter</code> to the pipeline keyed on a stable item identifier.</p> <pre><code>from chaser.pipeline.filters import DuplicateFilter\n\npipeline = Pipeline([\n    DuplicateFilter(key=lambda i: i.url),\n    JsonlStore(\"output.jsonl\"),\n])\nengine = Engine(frontier_db=\"crawl.db\", pipeline=pipeline)\n</code></pre>"},{"location":"guide/resume/#distributed-resume-with-redis","title":"Distributed resume with Redis","text":"<p>For multi-process crawls, use the Redis Frontier instead. It provides the same crash recovery guarantees over a shared Redis backend that all worker processes connect to.</p>"},{"location":"guide/testing/","title":"Testing","text":"<p>Chaser ships a <code>testing</code> module with helpers that let you unit-test Trappers without making real HTTP requests.</p>"},{"location":"guide/testing/#fakeresponse","title":"FakeResponse","text":"<p><code>FakeResponse</code> creates a <code>Response</code> object from a string of HTML (or JSON). Use it to feed your parse methods controlled input.</p> <pre><code>from chaser.testing import FakeResponse\n\nresponse = FakeResponse(\n    url=\"https://example.com/article/1\",\n    html=\"&lt;h1&gt;Hello&lt;/h1&gt;&lt;p class='body'&gt;World&lt;/p&gt;\",\n)\n\n# All the usual response methods work\nassert response.selector.css(\"h1::text\").get() == \"Hello\"\nassert response.status == 200\nassert response.ok\n</code></pre> <p>For JSON responses:</p> <pre><code>response = FakeResponse(\n    url=\"https://api.example.com/items\",\n    body=b'{\"count\": 3, \"items\": []}',\n    headers={\"Content-Type\": \"application/json\"},\n)\nassert response.json() == {\"count\": 3, \"items\": []}\n</code></pre>"},{"location":"guide/testing/#assert_items","title":"assert_items","text":"<p><code>assert_items</code> runs a parse method and checks that it yields exactly the expected items \u2014 in any order by default.</p> <pre><code>import pytest\nfrom chaser.testing import FakeResponse, assert_items\n\n\nclass QuoteItem(Item):\n    text: str\n    author: str\n\n\nclass QuoteTrapper(Trapper):\n    start_urls = [\"https://quotes.toscrape.com\"]\n\n    async def parse(self, response):\n        for quote in response.selector.css(\"div.quote\"):\n            yield QuoteItem(\n                text=quote.css(\"span.text::text\").get(\"\"),\n                author=quote.css(\"small.author::text\").get(\"\"),\n            )\n\n\n@pytest.mark.asyncio\nasync def test_parse_quotes():\n    html = \"\"\"\n        &lt;div class=\"quote\"&gt;\n            &lt;span class=\"text\"&gt;The only way out is through.&lt;/span&gt;\n            &lt;small class=\"author\"&gt;Robert Frost&lt;/small&gt;\n        &lt;/div&gt;\n        &lt;div class=\"quote\"&gt;\n            &lt;span class=\"text\"&gt;Stay hungry, stay foolish.&lt;/span&gt;\n            &lt;small class=\"author\"&gt;Steve Jobs&lt;/small&gt;\n        &lt;/div&gt;\n    \"\"\"\n    response = FakeResponse(url=\"https://quotes.toscrape.com\", html=html)\n    await assert_items(QuoteTrapper(), response, [\n        QuoteItem(text=\"The only way out is through.\", author=\"Robert Frost\"),\n        QuoteItem(text=\"Stay hungry, stay foolish.\", author=\"Steve Jobs\"),\n    ])\n</code></pre> <p>If the actual items don't match the expected ones, <code>assert_items</code> raises <code>AssertionError</code> with a diff.</p>"},{"location":"guide/testing/#testing-follow-up-requests","title":"Testing follow-up requests","text":"<p><code>assert_items</code> returns a tuple of <code>(items, requests)</code> \u2014 you can check the follow-up requests too:</p> <pre><code>@pytest.mark.asyncio\nasync def test_pagination():\n    html = \"\"\"\n        &lt;div class=\"quote\"&gt;&lt;span class=\"text\"&gt;...&lt;/span&gt;&lt;small class=\"author\"&gt;...&lt;/small&gt;&lt;/div&gt;\n        &lt;li class=\"next\"&gt;&lt;a href=\"/page/2/\"&gt;Next&lt;/a&gt;&lt;/li&gt;\n    \"\"\"\n    response = FakeResponse(url=\"https://quotes.toscrape.com\", html=html)\n    items, requests = await assert_items(QuoteTrapper(), response, expected_count=1)\n    assert len(requests) == 1\n    assert requests[0].url == \"https://quotes.toscrape.com/page/2/\"\n</code></pre>"},{"location":"guide/testing/#pytest-configuration","title":"pytest configuration","text":"<p>Chaser tests use <code>pytest-asyncio</code> in auto mode. Add this to your <code>pyproject.toml</code>:</p> <pre><code>[tool.pytest.ini_options]\nasyncio_mode = \"auto\"\n</code></pre>"},{"location":"guide/testing/#testing-the-pipeline","title":"Testing the pipeline","text":"<p>Test pipeline stages in isolation by calling <code>process</code> directly:</p> <pre><code>from chaser.pipeline.filters import DuplicateFilter\n\n\n@pytest.mark.asyncio\nasync def test_duplicate_filter():\n    f = DuplicateFilter(key=lambda i: i.url)\n    item = ArticleItem(url=\"https://example.com\", title=\"A\")\n\n    result1 = await f.process(item)\n    assert result1 is item            # first time \u2014 passes through\n\n    result2 = await f.process(item)\n    assert result2 is None            # duplicate \u2014 dropped\n</code></pre>"},{"location":"guide/trappers/","title":"Trappers","text":"<p>A Trapper is a class that defines two things: what to fetch (start URLs and any follow-up requests) and how to parse each response (extracting items and new URLs).</p>"},{"location":"guide/trappers/#basic-trapper","title":"Basic Trapper","text":"<pre><code>from chaser import Item, Request, Trapper\n\n\nclass ArticleItem(Item):\n    url: str\n    title: str\n    body: str\n\n\nclass BlogTrapper(Trapper):\n    start_urls = [\"https://blog.example.com\"]\n\n    async def parse(self, response):\n        for link in response.selector.css(\"article h2 a\"):\n            yield Request(\n                url=response.urljoin(link.attrib[\"href\"]),\n                callback=\"parse_article\",\n            )\n\n    async def parse_article(self, response):\n        yield ArticleItem(\n            url=response.url,\n            title=response.selector.css(\"h1::text\").get(\"\"),\n            body=\"\\n\".join(response.selector.css(\"article p::text\").getall()),\n        )\n</code></pre> <p><code>callback</code> routes the response to a different parse method. If omitted, <code>parse</code> is called for every response.</p>"},{"location":"guide/trappers/#responsefollow","title":"response.follow","text":"<p><code>response.follow</code> and <code>response.follow_all</code> are shortcuts for creating requests relative to the current page:</p> <pre><code>async def parse(self, response):\n    # single link from a selector result\n    link = response.selector.css(\"li.next a\")\n    if link:\n        yield response.follow(link)\n\n    # all matching links at once\n    yield from response.follow_all(css=\"div.listing a::attr(href)\")\n</code></pre>"},{"location":"guide/trappers/#crawltrapper","title":"CrawlTrapper","text":"<p><code>CrawlTrapper</code> auto-follows every <code>&lt;a&gt;</code> link within <code>allowed_domains</code>. Override <code>parse_item</code> to extract data from each page \u2014 link following is handled for you.</p> <pre><code>from chaser import CrawlTrapper, Item\n\n\nclass PageItem(Item):\n    url: str\n    title: str\n\n\nclass SiteCrawler(CrawlTrapper):\n    start_urls = [\"https://example.com\"]\n    allowed_domains = [\"example.com\"]\n\n    # optional: restrict to specific path patterns\n    allow_patterns = [r\"/blog/\", r\"/products/\"]\n    deny_patterns = [r\"/admin/\", r\"\\.pdf$\"]\n\n    async def parse_item(self, response):\n        yield PageItem(\n            url=response.url,\n            title=response.selector.css(\"title::text\").get(\"\"),\n        )\n</code></pre> Attribute Description <code>allowed_domains</code> Only follow links whose hostname matches one of these <code>allow_patterns</code> Regex list \u2014 only follow URLs matching at least one pattern <code>deny_patterns</code> Regex list \u2014 never follow URLs matching any pattern"},{"location":"guide/trappers/#sitemaptrapper","title":"SitemapTrapper","text":"<p><code>SitemapTrapper</code> reads <code>&lt;sitemapindex&gt;</code> and <code>&lt;urlset&gt;</code> XML files and queues every URL it finds. Override <code>parse_item</code> to parse individual pages.</p> <pre><code>from chaser import Item, SitemapTrapper\n\n\nclass ProductItem(Item):\n    url: str\n    name: str\n    price: str\n\n\nclass ShopTrapper(SitemapTrapper):\n    sitemap_urls = [\"https://shop.example.com/sitemap.xml\"]\n\n    async def parse_item(self, response):\n        yield ProductItem(\n            url=response.url,\n            name=response.selector.css(\"h1::text\").get(\"\"),\n            price=response.selector.css(\".price::text\").get(\"\"),\n        )\n</code></pre> <p>Nested sitemap indexes are followed recursively.</p>"},{"location":"guide/trappers/#custom-settings-per-trapper","title":"Custom settings per Trapper","text":"<pre><code>class MyTrapper(Trapper):\n    start_urls = [...]\n    custom_settings = {\n        \"download_delay\": 1.0,       # seconds between requests (this trapper only)\n        \"user_agent\": \"MyBot/1.0\",   # override User-Agent header\n    }\n</code></pre>"},{"location":"guide/trappers/#per-request-timeout","title":"Per-request timeout","text":"<p>Pass <code>meta={\"timeout\": N}</code> to override the engine-level timeout for a specific request:</p> <pre><code>yield Request(url=\"https://slow.example.com/report\", meta={\"timeout\": 120})\n</code></pre>"},{"location":"guide/trappers/#lifecycle-hooks","title":"Lifecycle hooks","text":"<p>Override <code>open</code> and <code>close</code> to manage resources (database connections, files, etc.) that live for the duration of the crawl:</p> <pre><code>class DbTrapper(Trapper):\n    async def open(self):\n        self.db = await connect_db()\n\n    async def close(self):\n        await self.db.close()\n\n    async def parse(self, response):\n        ...\n</code></pre>"},{"location":"guide/trappers/#running-multiple-trappers","title":"Running multiple Trappers","text":"<p><code>Engine.run</code> accepts a list of Trappers. All share the same connection pool and frontier:</p> <pre><code>engine = Engine(concurrency=16)\nitems = await engine.run([BlogTrapper(), ShopTrapper()])\n</code></pre>"}]}